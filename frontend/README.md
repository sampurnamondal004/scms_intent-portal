# SCMS Indent Portal Frontend

React + Tailwind frontend for the FastAPI indent backend.

## Run

```bash
npm install
npm run dev
```

The app uses `VITE_API_BASE_URL` for backend calls. By default it targets:

```text
http://localhost:8001
```
